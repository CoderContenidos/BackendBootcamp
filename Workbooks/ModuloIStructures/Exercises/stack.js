/**
 * 1. Realizar una función que, utilizando tu custom Stack, permite invertir una cadena.
 * 
 * 2. Realizar una función que, utilizando tu custmo Stack, permita corroborar el balance de 
 * símbolos de agrupación  () [] {}
 * 
 * Expresiones correctamente balanceadas:
 * "(({}))"
    "[[({})]]"
    "{[()]}"
    "({[]})"
 * Expresiones incorrectamente balanceadas:
    * "(()"
        "[(])"
        "{[({)}]}"
        "{{[()]]}"
 * 
 * El repositorio ya cuenta con un archivo de  resolución de ejercicios, por lo que
 * el repositorio no contará con elementos para evaluar tu resultado, dependerá de ti 
 * una autoevaluación de tu resultado.

 */